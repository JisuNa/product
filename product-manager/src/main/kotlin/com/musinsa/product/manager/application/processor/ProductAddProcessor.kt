package com.musinsa.product.manager.application.processor

import com.musinsa.product.domain.rdb.domain.repository.ProductRepository
import com.musinsa.product.manager.presentation.request.BrandAddRequest

class ProductAddProcessor(private val productRepository: ProductRepository) {
    fun execute() {

    }
}